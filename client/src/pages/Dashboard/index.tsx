import React from "react";
// Components
import { Sidebar, AppNavbar } from "../../components";

const Dashboard = () => {
  return (
    <section>
      <AppNavbar />
      <Sidebar />
    </section>
  );
};

export default Dashboard;
