import React from "react";
// Components
import { Sidebar } from "../../components";

const Dashboard = () => {
  return (
    <section>
      <Sidebar />
    </section>
  );
};

export default Dashboard;
